sacrifice_button_effect = {
	potential = {
        has_ethic = ethic_demon_cultists
	}
	allow = {
        custom_tooltip = {
            text = "requires_sacrifice_thralls"
            any_owned_pop = {
                NOT = { is_same_species = root }
            }
        }
	}

	effect = {
		random_owned_pop = {
			limit = { NOT = { is_same_species = root } }
            kill_pop = yes
		}
	}
}
